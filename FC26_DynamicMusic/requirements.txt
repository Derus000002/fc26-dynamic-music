# FC26 Dynamic Music — dipendenze Windows / Python 3.10+
# Installazione:  pip install -r requirements.txt

# --- Obbligatori ---
mss>=9.0.0              # screenshot veloci
Pillow>=10.0.0          # immagini + preview debug GUI
numpy>=1.26.0
opencv-python>=4.8.0    # computer vision (indicatore + preprocessing OCR)
# NOTA: pygame-ce AL POSTO di pygame classico.
# pygame classico non ha wheel precompilate per Python 3.14 -> pip prova a
# compilarlo da sorgente e fallisce (errore distutils/msvccompiler).
# pygame-ce e' il fork compatibile: si importa sempre come "import pygame",
# quindi il codice resta invariato. NON installare pygame e pygame-ce insieme.
pygame-ce>=2.5.3         # audio MP3/WAV/OGG con fade e start_time

# --- OCR: ne basta UNO (ordine di prova automatico) ---
# ATTENZIONE versioni Python:
#  - rapidocr-onnxruntime funziona SOLO su Python <= 3.12.
#  - Su Python 3.13/3.14 usa pytesseract + Tesseract installato (vedi README).
# Opzione A (consigliata su Python 3.11/3.12: leggera, no torch): rapidocr.
# Il marker fa installare rapidocr solo su Python <= 3.12 (sulle versioni
# successive pip la salta in automatico invece di fallire).
rapidocr-onnxruntime>=1.3.0; python_version <= "3.12"
# Opzione B (funziona su TUTTE le versioni di Python, richiede Tesseract .exe):
pytesseract>=0.3.10
# Opzione C (accurata ma pesante, richiede torch): easyocr
# easyocr>=1.7.0

# --- Utility ---
rapidfuzz>=3.0.0        # fuzzy matching nomi (Mbappé -> Mbappe)
keyboard>=0.13.5        # burst OCR alla pressione del tasto cambio + hotkey manuali
